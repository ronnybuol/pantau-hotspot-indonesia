{
  "name": "pantau-hotspot-indonesia",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "qc": "node scripts/validate_web_data.mjs",
    "build": "rm -rf dist && mkdir -p dist && cp -R src/* dist/ && cp -R data dist/data",
    "dev": "npm run build && npx wrangler dev",
    "deploy": "npm run qc && npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
